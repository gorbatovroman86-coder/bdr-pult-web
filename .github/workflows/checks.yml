name: Проверки

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  checks:
    name: Типы, линт, тесты, сборка
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Установка зависимостей
        run: npm ci

      - name: Проверка типов
        run: npx tsc -b --noEmit

      - name: Линт
        run: npm run lint

      # Регрессия эталонов A, B, C, сторожа граничных входов, серверные
      # маршруты на моках и автотесты экранов. Разошлось — дальше не идём.
      - name: Тесты
        run: npm test

      - name: Сборка
        run: npm run build
        env:
          PUBLIC_BASE: /${{ github.event.repository.name }}/
