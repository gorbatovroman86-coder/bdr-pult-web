# Зависимости и сборка
node_modules/
dist/
*.log

# ИСХОДНЫЕ ДАННЫЕ — в репозиторий не попадают никогда.
# Книга БДР и любые её снимки, выгрузки и производные.
*.xlsx
*.xls
*.xlsm
*SNAPSHOT*
/выгрузки/
/indep.py
*.dump.json
engine-*.json

# Инфраструктурные заметки: адреса серверов, порты, секретные пути
РАЗВЕДКА-ВПС.md

# СЕКРЕТЫ — не попадают в репозиторий никогда
access.md
.env
.env.*
*.key
*.pem
*secret*
*token*
credentials*
# Исключение: это дизайн-токены, а не токены доступа
!app/src/theme/tokens.css

.DS_Store

# Выгрузки пульта — рабочие файлы, в репозиторий не входят
bdr-params-*.json
bdr-journal-*.json
bdr-journal-*.csv

# База хранилища и всё, что её сопровождает
*.sqlite
*.sqlite-wal
*.sqlite-shm
/server/data/

# Адрес сервиса, поддомен, путевой префикс, логин и пароль —
# в репозиторий не попадают ни при каких условиях
.env.local
*.api-base
server/deploy/*.local
