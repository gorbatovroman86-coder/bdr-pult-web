# PROJECT — состояние проекта

Каноничный файл состояния. Здесь живёт «что сейчас» и «что дальше».
Всё остальное — по ссылкам, дублировать сюда не надо.

Обновлено: 2026-08-03, конец сессии по этапу 7.

---

## Что это

Веб-пульт сравнения пяти режимов работы маслоэкстракционного завода
(ядро ↔ масло, подсолнечник и рапс). Отвечает на один вопрос: **какой режим
ставить и насколько он выгоднее**.

Расчёт перенесён из книги «БДР (мотивация).xlsx» построчно, у каждой формулы
указана ячейка-источник.

- **Сайт:** https://gorbatovroman86-coder.github.io/bdr-pult-web/
- **Публичный репозиторий:** `gorbatovroman86-coder/bdr-pult-web` — здесь работа
- **Приватный `bdr-pult`:** история этапов 1–6, сайт из него не собирается
- **Локальная ветка `архив-этапы-1-5`:** полный архив со снимком книги
  и заметками по ВПС. **Не пушится никогда**

---

## Текущее состояние

**Этапы 1–7 закрыты.** Пульт опубликован и работает.

| Что | Состояние |
|---|---|
| Расчётное ядро | формулы строк 9–41 перенесены, у каждой ячейка-источник |
| Сходимость | эталоны A, B, C сходятся; эталон A сверен с книгой по 143 ячейкам, 0 расхождений |
| Тесты | 68, включая сторожа «экран ↔ эталон» и граничные входы |
| Интерфейс | живой: правка любого параметра пересчитывает пять режимов немедленно |
| Параметры | два уровня — общие на экране «Ставки и курсы», модельные в карточке режима |
| Экран сравнения | вердикт словом, рейтинг столбцами, отрывы, рентабельность, структуры, waterfall, Sankey, торнадо, каскад по капиталу |
| Публикация | пуш в `main` → проверки → сборка → Pages. Тесты упали — сайт не обновится |
| ФОТ | вынесен в параметр, в репозитории отсутствует, вводится в браузере |

Базовые значения воспроизводят рабочий эталон (C). Кнопка «Вернуть базовые
значения» возвращает ровно его — это проверяется тестом.

---

## Следующий шаг

Ничего не начато и не брошено на полпути. Приоритет следующей работы —
по решению владельца. Кандидаты, от самого полезного:

1. **Автоподтяжка курсов** CNY/RUB и USD/RUB с Мосбиржи (`iss.moex.com`)
   с фолбэком на ЦБ РФ (`cbr.ru`). Оба источника публичные, без ключей,
   с сервера проекта отвечают 200. Сейчас курс — редактируемое поле
   с пометкой источника; каркас есть, сетевого слоя нет.
2. **Автотесты интерфейса.** Расчёт покрыт (68 тестов), вёрстка и экраны —
   нет, проверяются управляемым браузером вручную.
3. **Общее хранилище параметров.** Сейчас всё в `localStorage` браузера:
   у разных людей могут быть разные цифры, и пульт об этом не знает.
   Единые цифры требуют бэкенда.
4. **Закрытый доступ.** Сайт публичный, без авторизации. Переезд на ВПС:
   домен, сертификат и caddy готовы, добавляется блок с basic auth.

---

## Открытые вопросы

Экономических открытых вопросов **нет** — все Q1…Q11 закрыты владельцем,
реестр в [QUESTIONS.md](QUESTIONS.md).

Известные ограничения модели и публикации — в [ОГРАНИЧЕНИЯ.md](ОГРАНИЧЕНИЯ.md),
читать до принятия решений по цифрам.

---

## Как работать

| Задача | Куда смотреть |
|---|---|
| Обновить ставки, курсы, цены, выходы | прямо в пульте, без кода. [ОБНОВЛЕНИЕ.md](ОБНОВЛЕНИЕ.md) |
| Понять, что автоматика, что руками | [ИСТОЧНИКИ.md](ИСТОЧНИКИ.md) |
| Понять ограничения модели | [ОГРАНИЧЕНИЯ.md](ОГРАНИЧЕНИЯ.md) |
| Изменить базу или формулы | `app/src/state/inputs.ts` (BASE), `app/src/domain/` |
| Повторить сверку с книгой | методика в [СВЕРКА-ЭТАП5.md](СВЕРКА-ЭТАП5.md) |
| Разбор книги и карта формул | [ЭТАП1-ОТЧЁТ.md](ЭТАП1-ОТЧЁТ.md) |
| Архитектура и типы | [SPEC-ЭТАП2.md](SPEC-ЭТАП2.md) |
| Дизайн-система и макеты | [ДИЗАЙН-МАКЕТЫ.md](ДИЗАЙН-МАКЕТЫ.md) |

---

## Правила, которые нельзя нарушать

- Книга БДР открывается **только на чтение**; снимок и выгрузки
  в репозиторий не входят.
- **Допуск сверки 0,011 тыс.₽ не ослаблять.** Он унаследован от округления
  эталонов в ТЗ и при переходе на живые данные подлежит ужесточению.
  При расхождении с эталоном — разбираться, а не подгонять допуск.
- Экономическую логику не «улучшать»: отличия от книги — только через явный
  флаг и с подписью в интерфейсе.
- Git только вперёд: без squash, rebase, amend и force. Ветка
  `архив-этапы-1-5` не пушится.
- ФОТ в публичный репозиторий не возвращать.
