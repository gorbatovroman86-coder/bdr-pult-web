# ЭТАП 2 — Спецификация

Основание: консолидированное ТЗ от 2026-08-03. Расчётное ядро не кодировано.
Эталон (C) сверен независимым пересчётом — **все 49 значений сходятся**,
расхождений нет. Заодно подтвердилось: **ДЗ считается по цене с НДС** — иначе
`%ден` по M3 не дал бы 3 264,06.

Дизайн и макеты — в [ДИЗАЙН-МАКЕТЫ.md](ДИЗАЙН-МАКЕТЫ.md).

---

## 0. Три вопроса, которые надо решить до кода

Нашёл при проектировании. Не «чиню молча», выношу.

### 0.1 🔴 Блокировка M3 по пошлине МСХ — модель не использует эту ставку

ТЗ §5: «БЛОКИРОВКА расчёта **M2/M3/M5**, если месяц ставки ≠ текущий».

Но **M3 не продаёт ни подсолнечного масла, ни подсолнечного жмыха.** Она продаёт
ядро, П/Ф и 3 категорию — а у них пошлина другая, 6,5 % множителем, к МСХ
отношения не имеющая. Подсолнечник у M3 — это сырьё на входе, а месячная
ставка МСХ применяется к продукту на выходе.

Проверка по эталону (C): цены M3 (59 891,25 / 50 789,25 / 42 687,25) не содержат
ни `DUTY_OIL = 7000`, ни `DUTY_MEAL = 1015,91`. Ставка МСХ на M3 не влияет никак.

**Что закладываю:** блокировка **выводится из фактической зависимости** — модель
блокируется тогда и только тогда, когда не задан параметр, который она реально
потребляет. По этому правилу М2 и М5 блокируются, **M3 — нет**.

**Нужно ваше решение:** оставить так (блокировать по зависимости) или всё-таки
жёстко блокировать и M3 тоже? Второе технически несложно, но тогда модель встанет
из-за числа, которое в её расчёте не участвует.

### 0.2 🟡 Строка 13 у M3: «не задано» против формулы `IF`

ТЗ §2 задаёт лузгу как `IF(стр.12>0; L1*N1*F4; 0)` для всех моделей. В файле
у M3 **строка 13 пустая целиком, формулы нет**. Если применить `IF` к M3,
условие выполнится (3 кат = 113,4 > 0) и тонны лузги станут **1 134 т/мес**,
а «Продажа, т» вырастет с 2 646 до 3 780.

На деньги это не влияет (цены лузги нет → выручка 0 в обоих случаях), эталоны
не затрагивает. Влияет только на отображаемые тонны.

**Что закладываю:** физический баланс массы (для «Раскладки тонны») считается
**всегда из выходов**, независимо от строки 13 — лузга там видна как масса
с явной подписью «не монетизируется». А поле «Продажа, т» воспроизводит файл
буквально, с пометкой. Так пульт показывает 1 134 т/мес лузги, которые файл
прячет, и при этом не расходится с ним.

### 0.3 🟡 «Стоимость переработки — параметр ПО МОДЕЛИ» против эталонов

ТЗ §6 требует сделать `H1` параметром по модели и пометить, что единая ставка
5 000 ₽/т смещает сравнение в пользу моделей с бо́льшим числом переделов.
Все три эталона при этом посчитаны на 5 000 ₽/т во всех пяти моделях.

**Что закладываю:** параметр по модели, **значение по умолчанию 5 000 у всех**
(эталоны сходятся), и постоянная видимая пометка о смещении на экране сравнения.
Расхождения нет, фиксирую, чтобы потом не выглядело как самодеятельность.

---

## 1. Структура приложения

Бизнес-логика полностью отделена от UI: `domain/` не импортирует ни React,
ни браузерные API. Это условие проверяемости эталонов.

```
bdr-pult/
├─ src/
│  ├─ domain/                    ← чистые функции, без React и без сети
│  │   ├─ types.ts               базовые типы, Sourced, Result
│  │   ├─ units.ts               форматирование чисел и единиц
│  │   ├─ yields.ts              баланс выходов, потери, производные доли
│  │   ├─ pricing.ts             нетбэк, два типа пошлины, цепочка CZCE
│  │   ├─ basis.ts               выбор лучшего базиса M1, точка переключения
│  │   ├─ engine.ts              строки 9–41: главный расчёт
│  │   ├─ capital.ts             строки 35–41: капитал и % пользования
│  │   ├─ scenarios.ts           сценарный аппарат
│  │   ├─ validation.ts          блокировки, «требуется ввод», устаревание
│  │   └─ formulaMap.ts          показатель → формула → лист!ячейка
│  ├─ data/
│  │   ├─ models.ts              5 наборов параметров (M1…M5)
│  │   ├─ defaults.ts            контрактные цены, логистика, ставки
│  │   ├─ references.ts          эталоны A, B, C
│  │   └─ storage.ts             localStorage + версия + миграции
│  ├─ sources/
│  │   ├─ fx.ts                  MOEX ISS → CBR, фолбэк и метки
│  │   ├─ duties.ts              ручной ввод ставок МСХ (слой подключаемый)
│  │   └─ czce.ts                ручной ввод котировки с месяцем контракта
│  ├─ ui/
│  │   ├─ App.tsx, routes.tsx
│  │   ├─ screens/               Сравнение, Модель, Сценарии, История, Формулы
│  │   ├─ components/            RibbonTonne, BasisRuler, ValueCell, …
│  │   └─ theme/                 токены, типографика
│  └─ main.tsx
├─ tests/
│  ├─ reference-A.test.ts        режим «как в Excel»
│  ├─ reference-B.test.ts        C5 = 1, НДС 1,2
│  ├─ reference-C.test.ts        рабочий эталон
│  ├─ basis.test.ts              точка переключения M1
│  ├─ yields.test.ts             баланс выходов и потери
│  └─ validation.test.ts         блокировки и устаревание
├─ БДР (мотивация).SNAPSHOT-2026-08-03.xlsx   (444, эталонный снимок)
└─ README.md
```

**Правило зависимостей:** `ui → domain`, `ui → sources`, `sources → domain`.
Обратных стрелок нет. `domain` не знает ни о localStorage, ни о fetch.

---

## 2. Схема данных и TypeScript-типы

### 2.1 Базовые типы

```ts
/** Провенанс значения. Отвечает на «откуда взялось и когда». */
export type Provenance =
  | { kind: 'file'; sheet: string; cell: string }        // перенос из БДР
  | { kind: 'manual'; enteredAt: string; by?: string }   // ручной ввод
  | { kind: 'auto'; source: 'MOEX' | 'CBR'; fetchedAt: string }
  | { kind: 'override'; enteredAt: string; replaces: Provenance } // подмена

/** Значение вместе с происхождением. `null` = НЕ ЗАДАНО (это не ноль). */
export interface Sourced<T> {
  value: T | null
  provenance: Provenance | null
  /** Месяц действия для месячных параметров: '2026-08'. */
  effectiveMonth?: string
  /** Дата фиксации и срок действия — для контрактных цен. */
  fixedAt?: string
  validUntil?: string
}

/** Результат, который может быть заблокирован. Никаких «тихих нулей». */
export type Result<T> =
  | { ok: true; value: T; warnings: Warning[] }
  | { ok: false; blockers: Blocker[] }

export interface Blocker {
  code: 'MISSING_INPUT' | 'STALE_MONTH' | 'NO_FX' | 'NO_QUOTE'
  paramId: string
  /** Что показать: «Не задана пошлина на подсолнечное масло за август 2026». */
  message: string
  /** Куда ведёт кнопка «Ввести». */
  fixAt: string
}

export interface Warning {
  code: 'CONTRACT_EXPIRING' | 'MANUAL_FX' | 'FILE_DISCREPANCY'
       | 'UNMONETIZED_MASS' | 'DOUBLE_COUNT' | 'SINGLE_PROCESSING_RATE'
  message: string
  /** Ссылка на пункт расхождений D1…D4 или на вопрос Q3…Q10. */
  ref?: string
}
```

### 2.2 Продукты, направления, валюты

```ts
export type ProductId =
  | 'kernel'    // ядро
  | 'semi'      // полуфабрикат
  | 'cat3'      // 3 категория
  | 'husk'      // лузга
  | 'oil'       // масло
  | 'meal'      // жмых

export type RawId = 'rapeseed' | 'sunflower'   // рапс | подсолнечник

/** Направление сбыта. Логистика привязана к паре продукт × направление. */
export type DestinationId = 'CN' | 'IR' | 'MY'  // Китай | Иран | Малайзия

export type Currency = 'RUB' | 'CNY' | 'USD' | 'EUR'
```

### 2.3 Пошлины — два типа ставки

```ts
export type DutyRate =
  | { kind: 'none' }
  /** 6,5 % от цены контракта. В файле — множитель 0,935. */
  | { kind: 'percent'; percent: number; actualAt: string }
  /** руб/т, месячная ставка МСХ. */
  | { kind: 'perTon'; rubPerTon: number; month: string }
```

Соответствие продуктам:

| Продукт | Тип ставки | Значение |
|---|---|---|
| рапсовое масло, рапсовый жмых | `none` | — |
| подсолнечное масло, подсолнечный жмых | `perTon` | ручной ввод, месячная |
| ядро, П/Ф, 3 категория | `percent` | 6,5 % (в файле 0,935) |

### 2.4 Цена: контракт → нетбэк

```ts
export interface ContractPrice {
  productId: ProductId
  destination: DestinationId
  amount: number            // цена контракта в валюте за тонну
  currency: Currency
  fixedAt: string
  validUntil?: string
}

export interface LogisticsLeg {
  productId: ProductId
  destination: DestinationId
  /** ЭКСПОРТНОЕ плечо: НДС 0 %, вычитается как есть. */
  rubPerTon: number
  month: string
}

/** Нетто-цена. ×1,1 и /1,1 сокращаются — работаем сразу с нетто. */
export function netback(input: {
  contract: ContractPrice
  fxRate: number
  duty: DutyRate
  logisticsRubPerTon: number
}): number
```

Формула:

```
base = contract.amount × fxRate
после пошлины:
  none    → base
  percent → base × (1 − percent/100)
  perTon  → base − rubPerTon
нетто    = после пошлины − logisticsRubPerTon
цена с НДС (только для показа) = нетто × 1,1
```

Порядок обязателен: пошлина и логистика вычитаются **в рублях после
конвертации**. Проверено на всех девяти позициях эталона (C).

### 2.5 Параметры модели

```ts
export type ModelId = 'M1' | 'M2' | 'M3' | 'M4' | 'M5'

export interface YieldSet {
  kernel: number          // F1 — задаётся
  cat3: number            // F3 — задаётся
  husk: number            // F4 — задаётся
  semi: number            // F2 — ПРОИЗВОДНАЯ: 1 − kernel − cat3 − husk
  oil: number             // F5 — задаётся
  meal: number            // F6 — ПРОИЗВОДНАЯ: (1 − loss) − oil
}

export interface ModelParams {
  id: ModelId
  name: string
  /** Имя листа для карты формул. У M3 — с концевым пробелом. */
  sheet: string
  raw: RawId

  intakeTonsPerDay: Sourced<number>       // L1
  daysPerMonth: Sourced<number>           // N1
  purchaseWithVat: Sourced<number>        // D1, ₽/т с НДС
  processingWithVat: Sourced<number>      // H1, ₽/т с НДС — ПО МОДЕЛИ
  moneyRate: Sourced<number>              // J1, доля в год

  /** Q9. По умолчанию как в файле: M3 = 0, остальные = 0,01. */
  lossShare: Sourced<number>

  yields: { kernel: Sourced<number>; cat3: Sourced<number>
            husk: Sourced<number>;   oil: Sourced<number> }

  /** M2 и M5: масло и жмых считаются от П/Ф, а не от сырья. */
  oilFromSemi: boolean

  /** ВНУТРЕННЕЕ плечо: H2/H3/H5/H6, НДС есть, делится на ставку услуг. */
  internalShipping: {
    semi: Sourced<number>          // H2
    kernelAndCat3: Sourced<number> // H3
    oil: Sourced<number>           // H5
    meal: Sourced<number>          // H6
  }

  /** Куда идёт каждый продаваемый продукт. У M1 масло — два кандидата. */
  routing: Partial<Record<ProductId, DestinationId | DestinationId[]>>
}
```

**Производные выходы — единственный источник истины:**

```ts
semi = 1 − kernel − cat3 − husk          // F2 = 1−F1−F3−F4
meal = (1 − lossShare) − oil             // F6 = 0,99−F5 при loss = 0,01
```

Обобщение `0,99 − F5` до `(1 − потери) − F5` даёт значения файла при
`loss = 0,01` (M1, M2, M4, M5) и `loss = 0` (M3). Эталоны сходятся.

### 2.6 Глобальные настройки расчёта

```ts
export interface CalcSettings {
  /** Ставка НДС на УСЛУГИ. По умолчанию 1.22. Режим 1.2 — для эталонов A и B. */
  serviceVatDivisor: Sourced<1.2 | 1.22>
  /** НДС на товар. Фиксировано 1.1, на результат не влияет. */
  goodsVatDivisor: 1.1
  taxRate: number                         // 0,25
  /** Экспортная пошлина РФ на ядро/П/Ф/3 кат. */
  kernelExportDuty: Sourced<{ percent: number; actualAt: string }>
  /** C5. В базовом режиме РОВНО 1. Не 0,8636. */
  forecastDiscount: number
  /** Режим сходимости с файлом. */
  mode: 'as-in-excel' | 'corrected'
  /** D4: ДЗ по всем продуктам вместо только ядра. Только в 'corrected'. */
  receivablesAllProducts: boolean
  calcDate: string
}
```

### 2.7 Результат расчёта

```ts
export interface MonthlyResult {
  modelId: ModelId
  month: string

  /** База всех удельных показателей. НИКОГДА не строка 9. */
  rawTons: number                         // L1 × N1

  /** Физический баланс массы — для «Раскладки тонны». */
  massBalance: Record<ProductId | 'loss', number>   // доли, сумма = 1

  tons: Record<ProductId, number>
  /** Строка 9 «как в файле». Показывать только с пометкой D1. */
  soldTonsAsInFile: number

  revenue: Record<ProductId, number>      // тыс.₽
  revenueTotal: number
  cost: number
  shipping: Record<ProductId, number>
  shippingTotal: number

  capital: { stock: number; payables: number
             receivables: number; total: number }   // ₽
  interestMonthly: number                 // тыс.₽
  tax: number
  netResult: number
  margin: number                          // доля

  /** Удельные — строго на тонну СЫРЬЯ. */
  perRawTon: { revenue: number; netResult: number }

  /** Для M1: какой базис выиграл и с каким отрывом. */
  basis?: BasisDecision

  warnings: Warning[]
}
```

---

## 3. Модель параметров: пять наборов

Общее для всех: `N1 = 27`, `J1 = 0,155`, `H1 = 5 000`, `H3 = 1 500`,
`H5 = 3 300`, `H6 = 1 100`.

| | M1 | M2 | M3 | M4 | M5 |
|---|---|---|---|---|---|
| лист | `Масло (рапс, Иран, Малайзия)` | `Масло (через ядро)` | `Ядро ` ⟵ пробел | `Масло (рапс, Китай)` | `Ядро+масло` |
| сырьё | рапс | подсолн. | подсолн. | рапс | подсолн. |
| D1 | 30 000 | 27 000 | 27 000 | 30 000 | 27 000 |
| L1 | 90 | 105 | 140 | 90 | 140 |
| F1 ядро | 0 | 0 | 0,40 | 0 | 0,20 |
| F3 3 кат | 0 | 0 | 0,03 | 0 | 0 |
| F4 лузга | 0 | 0,13 | 0,30 | 0 | 0,13 |
| **F2 П/Ф** ⟵ произв. | 0 | 0,87 | 0,27 | 0 | 0,67 |
| F5 масло | 0,36 | 0,49 | 0 | 0,36 | 0,49 |
| потери | 0,01 | 0,01 | **0** | 0,01 | 0,01 |
| **F6 жмых** ⟵ произв. | 0,63 | 0,50 | 0 | 0,63 | 0,50 |
| H2 отгр. П/Ф | 0 | 0 | **1 500** | 0 | 0 |
| `oilFromSemi` | нет | **да** | нет | нет | **да** |
| маршрут масла | **IR ∥ MY** | CN | — | CN | CN |
| маршрут жмыха | CN | CN | — | CN | CN |

Оговорка к M3: при `loss = 0` формула `meal = (1−0) − oil = 1 − 0 = 1`
дала бы жмых 100 %. В M3 масло и жмых не производятся вовсе, поэтому
у модели стоит флаг `producesOilLine: false`, и строки 14–15 равны нулю
по построению, как в файле. Это не исключение в формуле, а отсутствие передела.

---

## 4. Таблица источников

| Параметр | Тип | Источник | Периодичность | Блокирует |
|---|---|---|---|---|
| Курс CNY/RUB | 🔄 авто | MOEX ISS → CBR | при открытии | все |
| Курс USD/RUB | 🔄 авто | MOEX ISS → CBR | при открытии | M1 (Иран) |
| Пошлина подсолн. **масло**, ₽/т | ✋ вручную | МСХ, глазами | 1-го числа месяца | **M2, M5** |
| Пошлина подсолн. **жмых**, ₽/т | ✋ вручную | МСХ, глазами | 1-го числа месяца | **M2, M5** |
| Экспортная пошлина ядро/П/Ф/3 кат, % | ✋ вручную | 6,5 %, с датой | при изменении | M3, M5 |
| Котировка CZCE, CNY/т | ✋ вручную | виджет TV / CZCE | по решению | M4 |
| Месяц контракта CZCE | ✋ вручную | OIK/OIN/OIU + год | вместе с котировкой | M4 |
| Контрактные цены (7300, 1050, 8550, 1950, 6500, 5700, 4900, 2300) | ✋ вручную | договоры | при перезаключении | по продукту |
| Логистика по продукту × направлению | ✋ вручную | ставки перевозчиков | ежемесячно | по продукту |
| L1, N1, D1, H1, J1, выходы, H2/H3/H5/H6 | 📄 из файла | БДР, снимок | при пересмотре | — |
| Ставка НДС услуг | ⚙️ настройка | 1,22 по умолчанию | — | — |
| Потери, % | ⚙️ настройка | как в файле | — | — |

Обозначения в интерфейсе: 🔄 авто · ✋ вручную · 📄 из файла · ⚙️ настройка.
Значок всегда рядом со значением — **цвет не единственный маркер**.

---

## 5. Правила пересчёта фьючерса CZCE

Применяется **только к M4** (рапсовое масло, Китай). Больше нигде биржи нет.

### 5.1 Цепочка

```
котировка CZCE, CNY/т
   ↓ × kChinaDuty      (0,91 — пошлина КНР)
   ↓ × kChinaVat       (0,91 — НДС КНР)
   ↓ − portCNY         (100 — портовые расходы)
= цена контракта, CNY/т   ← это и есть «8000» из формулы
   ↓ × курс CNY/RUB
   ↓ − логистика 12 000 ₽/т
= нетто-цена, ₽/т
```

```ts
export interface CzceChain {
  quote: Sourced<number>              // котировка, CNY/т
  contractMonth: Sourced<string>      // 'OIK' | 'OIN' | 'OIU' + год
  kChinaDuty: number                  // 0,91 — видимый и редактируемый
  kChinaVat: number                   // 0,91 — видимый и редактируемый
  portCNY: number                     // 100  — видимый и редактируемый
}

export function quoteToContract(c: CzceChain): number   // прямая
export function contractToQuote(contract: number, c: CzceChain): number // обратная
```

### 5.2 Обратная проверка

```
(8000 + 100) / 0,91 / 0,91 = 9 782,03 CNY/т
```

Это встроенный тест: при вводе контрактной цены 8 000 пульт показывает,
какой котировке она соответствует, и это число должно попадать в реальный
диапазон CZCE. Если не попадает — предупреждение, не блокировка.

### 5.3 Ограничения по источнику

- Скрейпинг TradingView **запрещён** (ToS). В коде обращений к TV нет.
- Разрешено: официальный виджет TradingView (встраивание), официальные дневные
  котировки CZCE, **ручной ввод**.
- `OI1!` — непрерывный контракт, месяц через него не показать. Поэтому месяц
  контракта — **отдельное обязательное поле**: `OIK` май · `OIN` июль ·
  `OIU` сентябрь, плюс год.
- В интерфейсе всегда видно: авто или вручную, какой месяц, какая дата.

---

## 6. Правила выбора лучшего базиса в M1

### 6.1 Что сравнивается

Только **масло**. Жмых всегда идёт в Китай в юанях (`2300 CNY − 10 500 ₽/т`)
и от базиса масла не зависит — в M1 и M4 он идентичен.

```
Иран:     нетто_IR = 1050 × USD/RUB − логистика_IR
Малайзия: нетто_MY = 7300 × CNY/RUB − логистика_MY
```

Пошлины нет (рапс). Обе логистики сейчас 15 000 ₽/т, но это **параметр
по продукту и направлению**, а не константа — если ставки разойдутся,
формула ниже перестанет упрощаться, и это нормально.

### 6.2 Точка переключения — выводится, не зашивается

Общий случай (логистики разные):

```
Иран выгоднее ⟺ 1050 × USD − лог_IR  >  7300 × CNY − лог_MY
```

Частный случай (логистики равны, сокращаются) — то, что сейчас:

```
Иран выгоднее ⟺ USD/CNY > 7300 / 1050 = 6,9524
при CNY = 11,5 порог по доллару = 11,5 × 6,9524 = 79,95 ₽/долл
```

Проверено пересчётом: 6,9524 и 79,95 воспроизводятся.

```ts
export interface BasisDecision {
  winner: DestinationId
  loser: DestinationId
  netbackWinner: number          // ₽/т
  netbackLoser: number           // ₽/т
  gapRubPerTon: number           // отрыв, ₽/т
  gapNetResult: number           // отрыв в фин. результате, тыс.₽/мес
  fxUsed: { CNY: number; USD: number; provenance: Provenance }
  /** Кросс USD/CNY, при котором базисы равны. */
  crossoverUsdPerCny: number
  /** Текущий кросс. */
  currentUsdPerCny: number
  /** Порог по доллару при текущем юане, ₽/долл. */
  thresholdUsdRub: number
  /** Сколько рублей до переключения. Знак = в какую сторону. */
  distanceRub: number
  /** true, если победитель сменился с прошлого расчёта. */
  switched: boolean
}
```

### 6.3 Требования к показу

Базисы близки — переключение будет частым. Поэтому:

- Победитель назван **словом**, не только выделением.
- Отрыв показан **и в ₽/т, и в фин. результате** — второе понятнее для решения.
- Показаны **оба применённых курса** с источником и временем.
- Показано **расстояние до точки переключения** в рублях за доллар.
- Смена победителя относительно прошлого расчёта — **видимое событие**
  с плашкой, а не тихая подмена цифр.
- Проигравший базис не прячется: его нетбэк виден рядом.

---

## 7. Ручные и автоматические параметры — сводный список

### 🔄 Автоматические (2)

| Параметр | Источник | Фолбэк | Метка при подмене |
|---|---|---|---|
| CNY/RUB | MOEX ISS в торговые часы рабочего дня | CBR в выходные и при недоступности | «подставлено вручную» |
| USD/RUB | то же | то же | то же |

Показывать всегда: валюта, значение, **дата курса**, источник, время последнего
успешного обновления. Ручная подмена разрешена, но помечается и **за официальный
курс не выдаётся**.

### ✋ Ручные (обязательные, блокируют)

| Параметр | Ед. | Блокирует | Устаревание |
|---|---|---|---|
| Пошлина подсолн. масло | ₽/т | M2, M5 | месяц ≠ текущий |
| Пошлина подсолн. жмых | ₽/т | M2, M5 | месяц ≠ текущий |
| Логистика подсолн. масло → CN | ₽/т | M2, M5 | месяц ≠ текущий |
| Логистика подсолн. жмых → CN | ₽/т | M2, M5 | месяц ≠ текущий |
| Логистика рапс. масло → IR | ₽/т | M1 | месяц ≠ текущий |
| Логистика рапс. масло → MY | ₽/т | M1 | месяц ≠ текущий |
| Логистика рапс. масло → CN | ₽/т | M4 | месяц ≠ текущий |
| Логистика рапс. жмых → CN | ₽/т | M1, M4 | месяц ≠ текущий |
| Логистика ядро / П/Ф / 3 кат → CN | ₽/т | M3, M5 | месяц ≠ текущий |
| Котировка CZCE + месяц контракта | CNY/т | M4 | по решению |

### ✋ Ручные (со значением по умолчанию, не блокируют)

Контрактные цены — 7300 CNY, 1050 USD, 8000 CNY, 2300 CNY, 8550 CNY, 1950 CNY,
6500 CNY, 5700 CNY, 4900 CNY. Атрибуты поля: валюта, **дата фиксации**,
**срок действия**, предупреждение об устаревании.

Экспортная пошлина ядро/П/Ф/3 кат — 6,5 %, с датой актуальности.

### ⚙️ Настройки расчёта

НДС услуг (1,22 / 1,2) · потери по модели · стоимость переработки по модели ·
режим «как в Excel» / «скорректированный» · прогнозный дисконт (только в сценариях).

### ✅ Закрытые вопросы — полей «требуется ввод» не создают

Все вопросы закрыты 2026-08-03. Ни один не блокирует расчёт: у каждого есть
рабочее значение по умолчанию.

| Вопрос | Решение | Реализация |
|---|---|---|
| **Q3** | лузга не продаётся, выручка 0 | цена лузги пуста; масса видна в ленте с подписью «не монетизируется» |
| **Q5** | 5 000 ₽/т | параметр по модели, расшифровка скрытого листа — справочно, с пометкой о расхождении |
| **Q6** | **оба набора ФОТ актуальны** | справочный блок «проект» и «итого». Значения в репозитории отсутствуют — параметр с вводом в браузере. В фин. результат не входят, налог 25 % считается до них — как в файле. Выводов и оценок не делать |
| **Q7-бис** | второй передел отдельно не считать | 5 000 ₽/т сырья покрывают оба; постоянная пометка о смещении сравнения |
| **Q9** | потери — параметр | по умолчанию как в файле: M3 = 0, остальные = 0,01 |
| **Q10** | **месячная база** | годовые итоги **не показывать**; расхождение M9 ≠ ΣM10:M15 (24 057 против 26 462,7 т в M1 и M4) внесено в список известных ограничений, не исправляется |

**Единственные поля, требующие регулярного обновления** (не «требуется ввод»
из-за нерешённости, а нормальный рабочий цикл): месячные ставки пошлин МСХ,
логистика месяца, курсы валют.

---

## 8. Хранение

```ts
export interface StoredState {
  schemaVersion: 3
  settings: CalcSettings
  models: Record<ModelId, ModelParams>
  contracts: ContractPrice[]
  logistics: LogisticsLeg[]
  duties: { sunOil: Sourced<number>; sunMeal: Sourced<number>
            kernelPercent: Sourced<number> }
  czce: CzceChain
  fxCache: { CNY: Sourced<number>; USD: Sourced<number> }
  history: CalcSnapshot[]      // история расчётов
  scenarios: Scenario[]
}
```

localStorage, ключ `bdr-pult:v3`. При росте `schemaVersion` — миграция, старое
состояние сохраняется в `bdr-pult:v3:backup` до подтверждения. Архитектура
не завязана на localStorage: `storage.ts` — единственное место, где он
упоминается, замена на БД не трогает `domain/`.

**Секретов и ключей во фронтенде нет.** MOEX ISS и CBR — публичные, без ключей.

---

## 9. Карта формул: показатель → лист!ячейка

Каждый итоговый показатель раскрывается: формула, подставленные числа,
ссылка на исходную ячейку.

```ts
export interface FormulaTrace {
  label: string           // «Фин. результат»
  formula: string         // «Выручка − Себестоимость − Отгрузка − %денег − Налог»
  excelRow: number        // 32
  excelCells: string[]    // ['Ядро !B32']
  substituted: string     // «147 231,81 − 108 273,62 − 3 253,28 − 3 264,06 − 8 110,21»
  result: number
  /** Помечено, если отличается от файла. */
  deviation?: { from: string; reason: string; flagRef: string }
}
```

Полная карта строк 9–41 — в [ЭТАП1-ОТЧЁТ.md](ЭТАП1-ОТЧЁТ.md) §3, переносится
в `formulaMap.ts` один в один, с именем листа для каждой модели (у M3 — `Ядро `
с концевым пробелом).

**Не переносится:** служебный блок `O1:R6`, `Q11:Q12`, `O37`; блок 52–55
(показывается справочно); строки 34, 43, 50. Битая ссылка `R5 → H4`
зафиксирована в отчёте и **не чинится** — она в непереносимом блоке.

---

## 10. Валидация и блокировки

```ts
export function validate(state: StoredState, modelId: ModelId): Blocker[]
```

Правила:

1. **Зависимость, а не список.** Модель блокируется, если не задан параметр,
   который она реально потребляет (см. §0.1).
2. **Месячное устаревание.** Параметр с `effectiveMonth ≠ текущий месяц` →
   блокировка с сообщением «Нет ставки логистики на август 2026» и кнопкой ввода.
   **Прошлым месяцем не считать, старым значением не подменять.**
3. **Пустое ≠ ноль.** `value === null` → «требуется ввод». `value === 0` →
   считается как ноль, показывается как «0» с подписью «задано нулём».
   В интерфейсе это два разных состояния с разными значками.
4. **Устаревание контракта.** `validUntil` прошёл → предупреждение (не блокировка).
5. **Напоминание в новом месяце.** При первом открытии в новом месяце —
   плашка «Обновите ставки за <месяц>» со списком того, что просрочено.

---

## 11. Сценарный аппарат

```ts
export interface Scenario {
  id: string
  name: 'базовый' | 'оптимистичный' | 'пессимистичный' | string
  /** Только отклонения от базы. База не мутируется. */
  overrides: Partial<{
    fx: Partial<Record<Currency, number>>
    contractPrices: Partial<Record<ProductId, number>>
    yields: Partial<Record<ModelId, Partial<YieldSet>>>
    processingCost: Partial<Record<ModelId, number>>
    /** C5 живёт ТОЛЬКО здесь. В базовом расчёте его нет. */
    forecastDiscount: number
  }>
}
```

Прогнозный дисконт C5 — **явное поле только в сценариях**. В базовом расчёте
его нет и быть не может.

Три обязательных сценария для ЭТАПА 5: базовый · изменение цены масла ·
изменение выхода продукции.

---

## 12. Тесты

| Тест | Что проверяет | Настройки |
|---|---|---|
| `reference-A` | 5 моделей × 7 показателей = **35 значений** | C5 = 0,8636, НДС 1,2, старые цены |
| `reference-B` | 3 модели × 3 показателя = **9 значений** | C5 = 1, НДС 1,2, жмых 12 000 |
| `reference-C` | 7 строк × 7 показателей = **49 значений** | все решения, НДС 1,22 |
| `basis` | точка 6,9524 · порог 79,95 ₽ · смена победителя | — |
| `yields` | `semi = 1−F1−F3−F4` · `meal = (1−loss)−oil` для всех 5 | — |
| `validation` | блокировка по зависимости · «пусто ≠ ноль» · устаревание | — |
| `czce` | прямая и обратная цепочка, 9 782,03 | — |

Допуск: **0,01 тыс.₽** по денежным, **0,01 п.п.** по рентабельности.
При превышении — стоп и доклад, не обходить.

Эталон (C) уже воспроизведён мной вне приложения — все 49 значений сходятся,
так что тест заведомо проходим и служит регрессией, а не проверкой гипотезы.

---

## 13. Экраны

| Экран | Назначение |
|---|---|
| **Сравнение моделей** | главный. Пять строк-моделей на общей базе, сортировка по фин. результату |
| **Карточка модели** ×5 | полный БДР одной модели: тонны, выручка, затраты, капитал, раскрытие формул |
| **Сценарии** | базовый / оптимистичный / пессимистичный / пользовательский |
| **История расчётов** | снимки с датой, курсами и ставками на момент расчёта |
| **Детализация формул** | карта «показатель → формула → лист!ячейка» |
| **Ставки и курсы** | модуль пошлин, курсы, логистика, контрактные цены |

Графики (Recharts): прибыль · маржинальность · структура выручки · структура
затрат · waterfall от выручки к прибыли · чувствительность.

Макеты, токены и правила показа — в [ДИЗАЙН-МАКЕТЫ.md](ДИЗАЙН-МАКЕТЫ.md).

---

## 14. Чего в БДР нет — показывать как «не определено»

Энергозатраты · ГСМ · амортизация · постоянные расходы · EBITDA · операционная
прибыль · валовая прибыль · точка безубыточности · загрузка мощностей ·
оборачиваемость запасов · себестоимость по продуктам.

Всё производственное внутри `H1`. В интерфейсе — пометка «в БДР не определено»
либо не показывать вовсе. **Оценок не подставлять.**

---

## 15. Новая функциональность поверх файла — помечать в интерфейсе

Значок «➕ сверх БДР» рядом с каждым из:

1. Иранский базис
2. Выбор лучшего из двух базисов и точка переключения
3. Разложение подсолнечного жмыха на 1 950 CNY (в файле — рублёвые 12 000)
4. Месячные пошлины отдельным модулем
5. Параметр НДС услуг
6. Параметр потерь
7. Стоимость переработки по модели
8. Живые курсы вместо снимка 11,5
9. Цепочка CZCE и обратный пересчёт котировки

Всё остальное — перенос, эталон (A) сходится 1:1.
